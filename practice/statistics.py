def numbers (num):
    numsum = sum(num)
    nummin = min(num)
    nummax = max(num)
    mean = sum(num) / len(num)

    print("The sum is : " + str(numsum))
    print("The minimum is: " + str(nummin))
    print("The maximum is: " + str(nummax))
    print("The average is: " + str(mean))

    

     # type: ignore

